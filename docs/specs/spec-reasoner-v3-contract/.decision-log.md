# Decision Log

## 2026-06-20 — Create

- Reasoner v2는 mapping-only 기준으로 보존하고 v3에서 `uncertainty_option_index`를 모든 sample의 모델 생성 필드로 추가한다.
- unknown 문구 regex나 고정 label 규칙은 금지한다.
- 유효한 generated candidate가 없으면 임의 fallback 대신 `unresolved`로 submission을 차단한다.
- Coherence: 5개 capability가 각각 testable success를 가지며 non-goal과 제약이 구현 결정을 제한한다.
- Preservation: 조사에서 확인된 label-index 결함, raw 감사, v2 보존, test-derived 규칙 금지, Epic 3 fail-closed 요구를 SPEC 또는 companion에 보존했다. Wrapper-only content는 제외했다.

